import { partOne, partTwo } from './code';

describe('part 1', () => {
  it('should solve own input', () => {
    expect(partOne()).toBe(19358870);
  });
});

describe('part 2', () => {
  it('should solve own input', () => {
    expect(partTwo()).toBe(1143356492);
  });
});
