import { partOne, partTwo } from './code';

describe('part 1', () => {
  it('should solve own input', () => {
    expect(partOne().y).toBe(22877);
  });
});

describe('part 2', () => {
  it('should solve own input', () => {
    expect(partTwo().y).toBe(15210);
  });
});
