import { partOne, partTwo } from './code';

describe('part 1', () => {
  it('should solve own input', () => {
    expect(partOne()).toBe(3660);
  });
});

describe('part 2', () => {
  it('should solve own input', () => {
    expect(partTwo()).toBe(962913);
  });
});
