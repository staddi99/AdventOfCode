import { partOne, partTwo } from './code';

describe('part 1', () => {
  it.skip('should solve sample input', () => {
    expect(partOne(true)).toBe();
  });

  it.skip('should solve own input', () => {
    expect(partOne()).toBe();
  });
});

describe('part 2', () => {
  it.skip('should solve sample input', () => {
    expect(partTwo(true)).toBe();
  });

  it.skip('should solve own input', () => {
    expect(partTwo()).toBe();
  });
});
